# bring in brewer2mpl and gradient color generators
# change datasets to an array in main.js so they can be ordered
# have map and app modify the ids of their children so they
    can be just numbers of simple contect ignorant identifiers
# bring contour map (WhereWeMeet) functionality into main code
# consider other matplotlib2pygmaps helper functions
